If sensors have on-off support, its rendered in the setitngs dropdown, and accordingly, an on/off icon is 
rendered.

If it doesn't, the ON icon is used. Notwe that disable is supported since its a system feature.
